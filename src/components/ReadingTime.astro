---
import { READING_WPM, t, type BlogLang } from '../data/i18n';

interface Props {
  content: string;
  lang: BlogLang;
}

const { content, lang } = Astro.props;
const words = content.trim().split(/\s+/).length;
const wpm = READING_WPM[lang] ?? 200;
const minutes = Math.max(1, Math.round(words / wpm));
const label = `${minutes} ${t(lang).readingSuffix}`;
---

<span class="reading-time">{label}</span>

<style>
  .reading-time { color: var(--text-muted); font-size: 14px; }
</style>
